# Integration Test Configuration
# This file contains test-specific configuration for integration tests

# Test data configuration
# Set this to a real scan project name in your Contrast TeamServer for integration tests
test.scan.project-name=WebGoat
